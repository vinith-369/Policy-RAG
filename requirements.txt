# Vector database (in-memory mode — no Docker needed)
qdrant-client>=1.12.0

# Embeddings — dense
sentence-transformers>=3.0.0

# Embeddings — sparse BM25 (pure Python, no native deps — fastembed segfaults on Py3.14)
rank-bm25>=0.2.2

# LangChain + Ollama
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-ollama>=0.2.0
langgraph>=0.2.0

# Document loaders
pypdf>=5.0.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
